# Zeabur 项目配置
# 这个文件告诉 Zeabur 如何构建和运行项目

# 指定根目录为 backend
root: backend

# 构建配置
build:
  command: pip install -r requirements.txt

# 启动配置
start:
  command: uvicorn main:app --host 0.0.0.0 --port 8080

# 环境
environment: python
