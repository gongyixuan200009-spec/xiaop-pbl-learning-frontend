# Zeabur 部署配置
# 此文件用于 Zeabur 平台的自动部署

# 构建配置
[build]
framework = "nextjs"

# 环境变量（在 Zeabur 控制台配置）
[env]
# NEXT_PUBLIC_SUPABASE_URL = "your-supabase-url"
# NEXT_PUBLIC_SUPABASE_ANON_KEY = "your-supabase-anon-key"
# OPENAI_API_KEY = "your-openai-api-key"
